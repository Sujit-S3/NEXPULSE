# Store

Zustand stores for client-side state management.

## Purpose

This folder contains Zustand stores for managing client-side application state (auth, UI state, etc.).

## Status

Not yet implemented. Stores will be added as features require client-side state.
